using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Video;

public class SelectCinemaScripts : MonoBehaviour
{
/*    VideoPlayer CinemaPlayer;
    
    string films = PlayerPrefs.GetString("NameSelectFilms");
    void Start()
    {
        CinemaPlayer = GetComponent<VideoPlayer>();

        if (films == string.Empty)
        {
            films = "Ћюди на болоте";
        }
    }

    // Update is called once per frame
    void Update()
    {


        if (Input.GetKeyDown(KeyCode.E) && CinemaPlayer != null) CinemaPlayer.Play(); 
    }*/
}
